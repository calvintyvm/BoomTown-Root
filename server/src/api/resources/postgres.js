export default function(app){
// const { Pool } = require('pg')

// const pool = new Pool({
// user: app.get('PGUSER'),
// host: app.get('PGHOST'),
// database: app.get('PGDATABASE'),
// password: app.get('PGPASSWORD')})

// pool.query('SELECT * FROM items WHERE id = $1', [1], (err, res) => {
//   if (err) {
//     throw err
//   }

//   console.log('user:', res.rows[0])
// })

// return {
//     getItems(){
//         //pool query
//     }
// }

}